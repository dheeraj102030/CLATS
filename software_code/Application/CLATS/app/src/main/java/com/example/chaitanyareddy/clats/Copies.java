package com.example.chaitanyareddy.clats;

import android.widget.Toast;

/**
 * Created by Chaitanya Reddy on 09-Mar-17.
 */


public class Copies {
    /*String nameStr, name2Str, rollStr, emailStr
    //if (nameStr.equals("First Name")) {
      //  Toast nameFirst = Toast.makeText(AccountCreate.this, "Enter First Name", Toast.LENGTH_SHORT);
        nameFirst.show();
        //flag = 0;
    }
   // if (name2Str.equals("Last Name")) {
     //   Toast nameSec = Toast.makeText(AccountCreate.this, "Enter Last Name", Toast.LENGTH_SHORT);
        nameSec.show();
   // }
   // if (rollStr.equals("Roll Number")) {
        Toast roll = Toast.makeText(AccountCreate.this, "Enter valid roll no.", Toast.LENGTH_SHORT);
        roll.show();
    }
    if (emailStr.equals("Email ID")) {
        Toast emailToast = Toast.makeText(AccountCreate.this, "Enter valid Email Id", Toast.LENGTH_SHORT);
        emailToast.show();

    }
    // if (phoneStr.equals("Phone Number")) {
    // Toast phoneToast = Toast.makeText(AccountCreate.this, "Enter Valid Phone no.", Toast.LENGTH_SHORT);
    // phoneToast.show();
    //  flag = 0;
    // }
    Account A = new Account();
    A.setFname(FName);
    A.setCcolor(CycleColorStr);
    A.setCcompany(CycleNoStr);
    A.setCdes(CycleDesStr);
    A.setEmail(EmailID);
    A.setLname(LastName);
    A.setPass(Pass1Str);
    A.setRoll(Rollno);
    //A.setPhone(PhoneNo);

    handle.createAccount(A);

    EditText name = (EditText) findViewById(R.id.InputName);
        String nameStr = name.getText().toString();
        EditText pass = (EditText) findViewById(R.id.PasswordInput);
        String passStr = pass.getText().toString();

        String password = handle.SearchPass(nameStr);
        if(password.equals(passStr)){
            Intent i = new Intent(this, Mainpage2.class);
            startActivity(i);
        }
        else{
            Toast login = Toast.makeText(StudentSignup.this, "Incorrect name or password", Toast.LENGTH_SHORT);
            login.show();
        }

                i.putExtra("FirstName", nameStr);
                i.putExtra("LastName", name2Str);
                i.putExtra("RollNo.", rollStr);
                i.putExtra("EmailID", emailStr);
                i.putExtra("PhoneNo", phoneStr);

                   FName = (EditText)findViewById(R.id.TfFirstName);
        LName = (EditText) findViewById(R.id.TfLastName);
        RollNo = (EditText) findViewById(R.id.TfRollNo);
        Emailid = (EditText) findViewById(R.id.TfEmailId);
        PhoneNo = (EditText) findViewById(R.id.TfPhoneNo);
         Bundle bundle = new Bundle();
                                            bundle.putString("Present_location", jsonObject.getString("Present_location"));
                                            bundle.putString("Time", jsonObject.getString("Time"));
                                            bundle.putString("Past_location_1", jsonObject.getString("Past_location_1"));
                                            bundle.putString("Past_location_2", jsonObject.getString("Past_location_2"));
                                            bundle.putString("Name", jsonObject.getString("Name"));
                                            i.putExtras(bundle);

                                            B.setVisibility(View.VISIBLE);
        Pl.setVisibility(View.VISIBLE);
        Pl1.setVisibility(View.VISIBLE);
        Pl2.setVisibility(View.VISIBLE);
        T.setVisibility(View.VISIBLE);
        T1.setVisibility(View.VISIBLE);
        T2.setVisibility(View.VISIBLE);
        ridingCheck.setVisibility(View.VISIBLE);

        CheckPass.setVisibility(View.INVISIBLE);
        Pass.setVisibility(View.INVISIBLE);

*/

}
